/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.hafla.com', 'encrypted-tbn3.gstatic.com', "%20image.icons8.com", "image.icons8.com", "s3.ap-south-1.amazonaws.com", "book.hafla.com", "res.cloudinary.com"],
    }
};

export default nextConfig;